import React from "react";
import { Container } from "react-bootstrap";

const Metrics = () => {
    return (
        <Container>
            <h1>Metrics</h1>
        </Container>
    );
};

export default Metrics;