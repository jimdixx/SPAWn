import React from "react";
import { Container } from "react-bootstrap";

const Detection = () => {
    return (
        <Container>
            <h1>Detection</h1>
        </Container>
    );
};

export default Detection;
